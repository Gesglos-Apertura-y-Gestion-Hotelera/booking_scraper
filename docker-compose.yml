services:
  scraper:
    build:
      context: .
      dockerfile: dockerfile  # Aquí especificas cuál archivo usar
    shm_size: '2gb'                  # Crucial para evitar cierres de Chrome
    # El comando recibe el parámetro dinámicamente
    command: ["python", "src/main.py", "{{script_key}}"]
